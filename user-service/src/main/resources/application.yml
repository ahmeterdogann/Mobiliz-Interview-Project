server:
  port: 8083

spring:
  application:
    name: user-service
  datasource:
    url: jdbc:postgresql://localhost:5432/mobiliz
    username: postgres
    password: ahmet1903
    jpa:
      show-sql: true

logging:
  level:
    root: DEBUG

eureka:
  client:
    service-url:
      :defaultZone: http://localhost:8761/eureka/
